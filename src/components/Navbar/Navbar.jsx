import { NavLink } from "react-router-dom";

const Navbar = () => {
    return (
        <div>
            <nav>
                <NavLink></NavLink>
            </nav>
        </div>
    );
};

export default Navbar;